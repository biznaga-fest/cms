services:
  postgres:
    image: postgres:17
    container_name: biznagafest-cms
    environment:
      POSTGRES_DB: cms
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: Test1234
    ports:
      - "5432:5432"
    volumes:
      - postgresdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgresdata:
